import SwiftUI

@main
struct WhisperApp: App {
    var body: some Scene {
        WindowGroup("Whisper — Transcripción y traducción en vivo") {
            ContentView()
        }
        .defaultSize(width: 900, height: 560)
    }
}
